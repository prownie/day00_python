import time


def ft_progress(lst):
    for i in lst:
        i = i
    yield i

cpt = 0;
for i in range(1000):
    cpt += (i + 3) % 5
print("cpt = ",cpt)

listy = range(1000)
ret = 0
for elem in ft_progress(listy):
    ret += (elem + 3) % 5
    time.sleep(0.01)
print()
print("ret =",ret)
